name: CI Pipeline

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Build with Gradle
        run: bash gradlew clean build -x test

      # 🔢 VERSION
      - name: Calculate version
        run: |
          LAST_TAG=$(git tag --list "v1.0.*" --sort=-v:refname | head -n 1)

          if [ -z "$LAST_TAG" ]; then
            NEW_VERSION="v1.0.0"
          else
            PATCH=$(echo $LAST_TAG | cut -d'.' -f3)
            PATCH=$((PATCH + 1))
            NEW_VERSION="v1.0.$PATCH"
          fi

          echo "NEW_VERSION=$NEW_VERSION" >> $GITHUB_ENV

      # 🔐 Docker Login
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 🐳 Build image
      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/ci-cd-start:${{ env.NEW_VERSION }} .

      # 📤 Push version
      - name: Push version image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/ci-cd-start:${{ env.NEW_VERSION }}

      # 🏷️ Tag latest
      - name: Tag latest
        run: |
          docker tag ${{ secrets.DOCKER_USERNAME }}/ci-cd-start:${{ env.NEW_VERSION }} \
                     ${{ secrets.DOCKER_USERNAME }}/ci-cd-start:latest

      # 📤 Push latest
      - name: Push latest image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/ci-cd-start:latest

      # 🏷️ Git tag push
      - name: Create Git tag
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git tag ${{ env.NEW_VERSION }}
          git push origin ${{ env.NEW_VERSION }}

      # 🚀 Deploy (ONLY THIS SERVICE)
      # 🚀 Deploy (ONLY THIS SERVICE)
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: root
          key: ${{ secrets.SSH_KEY }}
          script: |
            docker compose pull ci-cd-start
            docker compose up -d --no-deps ci-cd-start